# Image-Based-Encryption-System
MATLAB-based image encryption system that generates dynamic keys from image pixel values combined with a date-based offset for secure message encryption and decryption.
